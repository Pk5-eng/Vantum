[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["cd shared && npx tsc && cd ../backend && npx tsc"]

[start]
cmd = "node backend/dist/index.js"
